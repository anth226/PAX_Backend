{{> header }}
    <h4 class="bodyheader">
        {{#equals lang "en"}}
            SubArea List
        {{else}}
            সাবখাত তালিকা
        {{/equals}}
    </h4>
    <table>
        <thead>
            <tr>
            {{#each headerColumns as | header |}}
                <th class="tableHeader">{{ header }}</th>
            {{/each}}
            </tr>
        </thead>
        <tbody>
            {{#equals lang "en"}}
                 {{#each data as | item |}}
                    <tr>
                        <td>{{inc @index}}</td>
                        <td>
                            {{ item.name_en }}
                        </td>
                        <td>{{number item.allocated_amount }}</td>
                        <td>{{ item.comments }}</td>
                        {{!-- <td>{{ item.areaInfo.name_en }}</td> --}}
                    </tr>
                {{/each}}
            {{else}}
                {{#each data as | item |}}
                    <tr>
                        <td>{{inc @index}}</td>
                        <td> {{ item.name_bn }}</td>
                        <td>{{number item.allocated_amount }}</td>
                        <td>{{ item.comments }}</td>
                        {{!-- <td>{{ item.areaInfo.name_bn }}</td> --}}
                    </tr>
                {{/each}}
            {{/equals}}
        </tbody>
    </table>
 {{> footer }}